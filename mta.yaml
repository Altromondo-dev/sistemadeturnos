_schema-version: "3.2"
ID: transenersistemadeturnos
description: Generated by Fiori Tools
version: 1.0.1
modules:
- name: transenersistemadeturnos-destination-content
  type: com.sap.application.content
  requires:
  - name: transenersistemadeturnos-destination-service
    parameters:
      content-target: true
  - name: transenersistemadeturnos-repo-host
    parameters:
      service-key:
        name: transenersistemadeturnos-repo-host-key
  - name: transenersistemadeturnos-uaa
    parameters:
      service-key:
        name: transenersistemadeturnos-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: transenersistemadeturnos_html_repo_host
          ServiceInstanceName: transenersistemadeturnos-html5-srv
          ServiceKeyName: transenersistemadeturnos-repo-host-key
          sap.cloud.service: transenersistemadeturnos
        - Authentication: OAuth2UserTokenExchange
          Name: transenersistemadeturnos_uaa
          ServiceInstanceName: transenersistemadeturnos-xsuaa-srv
          ServiceKeyName: transenersistemadeturnos-uaa-key
          sap.cloud.service: transenersistemadeturnos
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: transenersistemadeturnos-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: transenersistemadeturnos-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - transenersistemadeturnos.zip
      name: transenersistemadeturnos
      target-path: resources/
- name: transenersistemadeturnos
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: transenersistemadeturnos-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: ignore
      version: 1.0.0
    service: destination
    service-name: transenersistemadeturnos-destination-service
    service-plan: lite
- name: transenersistemadeturnos-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: transenersistemadeturnos-xsuaa-srv
    service-plan: application
- name: transenersistemadeturnos-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: transenersistemadeturnos-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
